# Curation record

The portfolio-facing `main` branch was derived from the private coursework
repository while its exact pre-curation grading tree remained recoverable at
private `legacy` commit:

```text
f0f731f3098988446843a2a698612ac3f2a4285d
```

The private curated source `main` is at
`ee587a13ec66cb3f61529050fc66231e186a769d`. Neither private ref is published
to this repository.

On 2026-09-05, the owner requested that all recoverable original submitted
reports and presentation documents be restored to the public portfolio. Seven
student-authored files were copied byte-for-byte and given neutral public path
names. [`SUBMISSIONS.md`](SUBMISSIONS.md) records their exact sizes and hashes.

The earlier final-project `FinalProject.pptx` was not restored: source history
shows that the `finish final` commit removed it and added `team20.pptx`, making
the latter the final submitted deck. The accompanying MKV is presentation
video, not a report document, and remains private. Homework handouts, lecture
notes, report templates, profiling references, seminar material, and generated
media are likewise outside the document-restoration scope.

The exact documents intentionally retain submitted identity data. Portfolio
source remains organized separately from the unmodified grading-time artifacts
under `submission/`.
